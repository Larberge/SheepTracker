import { Dimensions } from "react-native";

export default {
  themeRadius: 12,
  buttonBorderWidt: 4,
  standardComponentSize: 52,
  iconSize: 32,
  borderSize: 2,
  screenPadding: 32,
  windowWidth: Dimensions.get("screen").width,
  windowHeight: Dimensions.get("screen").height
};
